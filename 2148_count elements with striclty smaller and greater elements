class Solution:
    def countElements(self, nums: list[int]) -> int:
        a=sorted(nums)
        mi=a.count(min(a))
        ma=a.count(max(a))
        if 0<len(a)<=2 or len(set(a)) == 1:
            return 0
        else:
            return len(a)-mi-ma