class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        a=len(nums)/2
        c=Counter(nums)

        for key , value in c.items():
            if value > a:
                return key