class Solution:
    def getCommon(self, nums1: List[int], nums2: List[int]) -> int:
        common = list(set(nums1) & set(nums2))
        
        if len(common)>=1:
            return min(common)
        else:
            return -1