class Solution:
    def maxProductDifference(self, nums: List[int]) -> int:
        a=sorted(nums,reverse=True)
        a1=a[0]
        a2=a[1]
        a3=a[-1]
        a4=a[-2]

        return (a1*a2-a3*a4)