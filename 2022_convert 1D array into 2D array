class Solution:
    def construct2DArray(self, original: List[int], m: int, n: int) -> List[List[int]]:
        mat=[]
        if m*n !=len(original):
            return []

        for i in range(m):
            start=i*n
            end=start+n
            mat.append(original[start:end])
        return mat