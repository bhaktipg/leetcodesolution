class Solution:
    def missingNumber(self, nums: List[int]) -> int:
        a=0
        n=len(nums)
        total=int((n*(n+1))/2)

        for i in nums:
            a+=i
        
        return total-a
