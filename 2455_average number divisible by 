class Solution:
    def averageValue(self, nums: List[int]) -> int:
        s=0
        c=0

        for i in range(len(nums)):
            if nums[i]%6==0:
                s=s+nums[i]
                c=c+1
        if s==0:
            return 0
        else:
            return int(s/c)