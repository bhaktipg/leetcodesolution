class Solution:
    def shuffle(self, nums: List[int], n: int) -> List[int]:
        a = []
        i = 0
        j = n

        while i < n:
            a.append(nums[i])
            a.append(nums[j])
            i += 1
            j += 1

        return a