class Solution:
    def kthDistinct(self, arr: List[str], k: int) -> str:
        count= Counter(arr)
        a=[]

        for key , value in count.items():
            if value==1:
                a.append(key)
     
        if k >len(a):
            return ""
        else:
            return a[k-1]