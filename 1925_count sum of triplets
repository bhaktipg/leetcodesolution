class Solution:
    def countTriples(self, n: int) -> int:
        count=0
        for a in range(1,n):
            for b in range(2,n+1):
                res=a*a+b*b
                c=sqrt(res)
                if c in range(1, n + 1):
                    count=count+1
        return count
        