class Solution:
    def dominantIndex(self, nums: List[int]) -> int:
        target=max(nums)
        ind=nums.index(target)
        for i in range(len(nums)):
            if nums[i]*2 > target and ind!=i:
                return -1
        return ind
