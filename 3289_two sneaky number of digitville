class Solution:
    def getSneakyNumbers(self, nums: List[int]) -> List[int]:
        count=Counter(nums)
        a=[]

        for key , value in count.items():
            if value>1:
                a.append(key)
        return a
        