class Solution:
    def findLucky(self, arr: List[int]) -> int:
        counts = {}

        for item in arr:
            counts[item] = counts.get(item, 0) + 1

        ans = -1

        for key, value in counts.items():
            if key == value:
                ans = max(ans, key)

        return ans