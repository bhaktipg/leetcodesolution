# Write your MySQL query statement below
select query_name, round(sum(rating/position)/count(distinct result),2) as quality , round(SUM(CASE WHEN rating < 3 THEN 1 ELSE 0 END)*100/count(distinct result),2) as poor_query_percentage
from Queries q
group by query_name