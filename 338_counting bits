class Solution:
    def countBits(self, n: int) -> List[int]:
        a=[]
        
        for i in range(n+1):
            c=0
            bina=bin(i)[2:] 
            
            for j in bina:
                if j=="1":
                    c=c+1
            a.append(c)
                
        return a