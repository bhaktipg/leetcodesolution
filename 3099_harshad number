class Solution:
    def sumOfTheDigitsOfHarshadNumber(self, x: int) -> int:
        s=list(str(x))
        s1=0
        for i in range(len(s)):
            s1=s1+int(s[i])
        if x%s1==0:
            return s1
        else:
            return -1