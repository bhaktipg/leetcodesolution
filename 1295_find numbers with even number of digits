class Solution:
    def findNumbers(self, nums: List[int]) -> int:
        a=0

        for i in nums:
            length=len(str(i))
            if length%2==0:
                a=a+1
            
        return a