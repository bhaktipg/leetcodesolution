class Solution:
    def arraySign(self, nums: List[int]) -> int:
        a=1
        for i in range(len(nums)):
            a=a*nums[i]

        if a >0:
            return 1
        elif a <0:
            return -1
        else:
            return 0
        