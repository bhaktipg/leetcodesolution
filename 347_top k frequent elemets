class Solution:
    def topKFrequent(self, nums: List[int], k: int) -> List[int]:
        c=Counter(nums)
        c1=c.most_common(k)
        a=[]
        for i in range(len(c1)):
            a.append(c1[i][0])
        return a