class Solution:
    def isAcronym(self, words: List[str], s: str) -> bool:
        a=[]

        for i in range(len(words)):
            s1=words[i][0]
            a.append(s1)
        c= "".join(a)
        
        if str(c)==s:
            return True
        else:
            return False