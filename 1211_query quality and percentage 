# Write your MySQL query statement below
select query_name, round(sum(a.rating/a.position)/count(*),2) as quality, round(sum(a.rating<3)*100/count(*),2) as poor_query_percentage 
from Queries a
group by query_name
